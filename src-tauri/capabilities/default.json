{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for gh-client desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
